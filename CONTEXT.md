# 懂宝 · 领域模型与上线共识（CONTEXT）

> 本文是"快速上线"方案的定稿记录。术语以此为准，改动先改这里。

## 领域术语

| 术语 | 英文/代码 | 定义 |
|---|---|---|
| 用户 | `User` | 微信登录者。openid 唯一。登录即注册，无密码无手机号 |
| 家庭 | `Family` | 数据隔离边界。注册时每户自动一个；宝宝、记录、媒体的 `family_id` 都挂在它下面 |
| 宝宝 | `Baby` | 被记录对象。上线版一个账号一个宝宝（UI 不做切换，表结构天然支持以后加） |
| 记录 | `Record` | 宝宝事件（喂奶/辅食/睡眠/排便/尿布/哭闹/疫苗/用药/身高体重/自定义）。软删可撤销 |
| 草稿 | `RecordDraft` | 语音/拍照来源的待确认记录，确认后转为记录 |
| 凭证 | token | JWT（HS256，30 天）。服务端零存储；吊销方式 = 鉴权时查用户表，用户不存在即 401 |

## 决策记录（ADR）

- `docs/adr/0001-pull-login-into-mvp.md` — 登录体系从 Phase 5 提前进 MVP，共享/多家长仍留 Phase 5
- `docs/adr/0002-stateless-jwt-no-redis.md` — 无状态 JWT，不引 Redis，吊销靠鉴权查用户表

## 上线决策（2025-09 定稿）

1. **范围**：微信登录 + 首页 + 记录页 + 宝宝档案页。隐藏 AI/哭声/文章/统计图表；语音拍照保留、识别未配置时降级手动填。
2. **登录**：`wx.login` code → `jscode2session` 换 openid → 查/建 User+Family → 发 JWT。静默重登，无登录页 UI。
3. **JWT 不用 Redis**：自包含验签 + 鉴权时查用户表（顺带实现注销后立即失效）。需要短信验证码/强制下线时再引 Redis。
4. **数据**：`users`、`families` 两表；存量硬编码 `FAMILY_ID` 常量删除，全部改由 token 解析。
5. **记录页按原型 03**：7 天日期条 + 任意日期选择 + 类型筛选（全部/喂奶/辅食/睡眠/排便/尿布）+ 时间线 + 点按编辑 + ＋添加。日期按客户端本地过滤，不加后端参数。
6. **首页按原型 01**：宝宝行(→档案) + 问候语 + 今日记录指标 + 快速记录 + 最近动态 3 条(查看全部→记录页)。AI/哭声卡与文章推荐区块不做。
7. **档案页按原型 09**：查看 + 编辑 + 注销账号按钮（微信平台合规要求）。注销删 user/family/宝宝/记录/媒体文件。
8. **平台**：微信小程序，测试号/开发版阶段；开发工具勾选"不校验合法域名"，域名+HTTPS 后补。部署到云服务器（docker compose）。
9. **配置**：`WECHAT_APPID`、`WECHAT_SECRET`、`JWT_SECRET`（默认开发值，生产必须覆盖）环境变量。
10. **隐私政策**：按实际收集字段起草（宝宝昵称/生日/性别、记录内容、语音/照片），贴小程序后台。
11. **推迟**：多宝宝切换、家庭共享、退出登录、头像昵称同步（Phase 5 一并考虑）。

## 冲突提示

与 PRD Phase 划分的冲突已由 **ADR-0001** 正式修订：仅「登录」提前，「家庭共享/多家长」仍留 Phase 5。
